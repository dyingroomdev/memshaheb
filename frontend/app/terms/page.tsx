import { DynamicPage } from "@/components/DynamicPage";

export const metadata = {
  title: "Terms & Conditions — Memshaheb",
};

export default function TermsPage() {
  return <DynamicPage slug="terms" />;
}
